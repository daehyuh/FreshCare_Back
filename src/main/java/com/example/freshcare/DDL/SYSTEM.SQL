CREATE TABLE LAUNDRYROOM(
                            LAUNDRYIDX NUMBER(20) PRIMARY KEY,
                            LAUNDRYNAME VARCHAR2(100)
);
CREATE SEQUENCE LAUNDRY_SEQ START WITH 0 MINVALUE 0;

select * from LAUNDRYROOM;

INSERT INTO LAUNDRYROOM VALUES(0, 0);

CREATE TABLE MACHIEN(
                        MACHIENIDX NUMBER(20) PRIMARY KEY,
                        LAUNDRYIDX NUMBER(20) REFERENCES LAUNDRYROOM(LAUNDRYIDX),
                        USERIDX NUMBER(20) REFERENCES USERTABLE(USERIDX),
                        MACHIENNAME VARCHAR2(100) NOT NULL,
                        MACHIENTYPE NUMBER(1) CHECK (MACHIENTYPE IN(0, 1)),
                        WASHTIME DATE
);

DESC MACHIEN;

DROP TABLE MACHIEN;
INSERT INTO MACHIEN VALUES(MACHIEN_SEQ.nextval, 0, NULL,'1','0', SYSDATE);
INSERT INTO MACHIEN VALUES(MACHIEN_SEQ.nextval, 0, NULL,'2','0', SYSDATE);
INSERT INTO MACHIEN VALUES(MACHIEN_SEQ.nextval, 0, NULL,'3','1', SYSDATE);
commit;
drop table MACHIEN;
select * from MACHIEN;

COMMIT;
CREATE SEQUENCE MACHIEN_SEQ START WITH 0 MINVALUE 0;

drop SEQUENCE MACHIEN_SEQ;

CREATE TABLE USERTABLE(
                          USERIDX NUMBER(20) PRIMARY KEY,
                          USERROOM VARCHAR2(100) UNIQUE NOT NULL,
                          USEREMAIL VARCHAR2(100) UNIQUE NOT NULL,
                          USERPHONE VARCHAR2(100) UNIQUE NOT NULL,
                          USERPW VARCHAR2(100) NOT NULL
);

insert into USERTABLE values(USER_SEQ.nextval,'710-2','rkdeown120@naver.com','010382222413','12122');
select * from USERTABLE;

CREATE SEQUENCE USER_SEQ START WITH 0 MINVALUE 0;
--DROP TABLE USERTABLE CASCADE CONSTRAINTS ;
--DROP TABLE USER_SEQ;

ALTER SESSION SET NLS_DATE_FORMAT = 'HH24:MI:SS';
